# Server Configuration
PORT=5001
NODE_ENV=development

# Firebase Admin SDK (choose ONE method):
#
# Option 1: Use Application Default Credentials (recommended for Cloud Run)
#   - No env var needed, uses `gcloud auth application-default login` locally
#   - Uses Workload Identity on Cloud Run
#
# Option 2: Service Account File Path (for local dev only)
#   GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/serviceAccountKey.json
#
# Option 3: Inline service account JSON (for CI/CD)
#   FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
